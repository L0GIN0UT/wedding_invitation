# FastAPI Backend - Wedding Invitation

## 📁 Структура проекта

```
Main_back/
├── main.py                 # Точка входа FastAPI приложения
├── routers/                # Роутеры (эндпоинты)
│   └── auth.py            # Роутер авторизации
├── schemas/                # Pydantic схемы (запросы/ответы)
│   └── auth.py            # Схемы для авторизации
├── services/               # Бизнес-логика
│   ├── verification.py    # Сервис верификации кодов
│   ├── session.py         # Сервис управления сессиями
│   └── guest.py           # Сервис работы с гостями
├── dependencies/           # Зависимости (DI)
│   └── redis.py           # Redis клиент
├── requirements.txt        # Python зависимости
└── Dockerfile             # Docker образ
```

## 🚀 Запуск

### Через Docker Compose

```bash
docker-compose up -d api
```

### Локально

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## 📡 API Эндпоинты

### Авторизация

#### 1. Отправка кода верификации
```
POST /auth/send-code
Body: {
  "phone": "+79277899017"
}
```

#### 2. Подтверждение кода
```
POST /auth/verify-code
Body: {
  "phone": "+79277899017",
  "code": "123456"
}
Response: {
  "success": true,
  "token": "session_token_here",
  "message": "Код подтвержден, сессия создана"
}
```

## 🔧 Настройки

Настройки находятся в `conf/settings.py`:

- `VERIFICATION_CODE_LENGTH` - длина кода (по умолчанию 6)
- `VERIFICATION_CODE_TTL` - время жизни кода в секундах (по умолчанию 300 = 5 минут)
- `SESSION_TOKEN_TTL` - время жизни сессии в секундах (по умолчанию 86400 = 24 часа)
- `REDIS_HOST`, `REDIS_PORT` - настройки Redis

## 📝 Особенности

- ✅ Коды верификации хранятся в Redis
- ✅ При каждом запросе генерируется новый код (старый удаляется)
- ✅ Коды одноразовые (удаляются после использования)
- ✅ Сессии хранятся в Redis
- ✅ Проверка существования гостя в базе данных

